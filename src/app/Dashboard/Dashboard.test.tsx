import "@testing-library/jest-dom";

describe("MAIN LAYOUT", () => {
  // template test to verify workflow
  test("first test to check out GITHUB Actions", () => {
    expect(2 + 1).toEqual(3);
  });
});
